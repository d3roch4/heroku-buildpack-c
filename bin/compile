#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

set -e
set -o pipefail

BUILD_DIR=$1
CACHE_DIR=$2

mkdir $BUILD_DIR
cd $BUILD_DIR
cmake $ENV_DIR
echo "-----> Configuring CMake"

# make
echo "-----> Compiling with Make"
make
